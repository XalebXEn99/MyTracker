# Android stub

This directory is a placeholder for Android-specific implementation.

The React Native bare workflow app includes Android support plans, but the current project is primarily configured for iOS CI and unsigned IPA export.

To add Android support, create standard `android/app` Gradle files and update native modules accordingly.
